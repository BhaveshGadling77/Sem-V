bootloader:
    bootmain.c 
    bootasm.S
    entry.S

kernel:
  -  kalloc.c
  - fork function code.
  - exec syscall code.
  - open,read, dup, dup2 questions
  - yield(), sched(), scheduler();
  - trap(), alltraps(), vector.S
 - syscall.c
    
 -  kernel main.c
 - swtchkvm() wwhat it does not how
    
 - struct proc, ptable and 

 - file.c exec.c
 - this is in the slides.

other than this 
Makefile.



